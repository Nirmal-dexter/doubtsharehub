services:
  - type: web
    name: doubt-share-hub
    env: static
    buildCommand: yarn install && yarn build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
